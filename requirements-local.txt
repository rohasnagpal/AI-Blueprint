# Local RAG dependencies
# On Python 3.13+ (no onnxruntime wheels), install with --no-deps:
#
#   pip install chromadb --no-deps
#   pip install llama-index-core llama-index-vector-stores-chroma \
#               llama-index-embeddings-huggingface sentence-transformers \
#               torch transformers huggingface_hub tokenizers \
#               nltk dirtyjson tenacity deprecated numpy --no-deps
#
# Python 3.12 or earlier: pip install chromadb && pip install -r requirements-local.txt

llama-index-core>=0.12.0
llama-index-vector-stores-chroma>=0.4.0
llama-index-embeddings-huggingface>=0.1.0
sentence-transformers>=2.2.0
torch
transformers
huggingface_hub
tokenizers
nltk
dirtyjson
tenacity
deprecated
numpy
